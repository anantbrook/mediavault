version: '3.8'

services:
  redis:
    image: redis:latest
    ports:
      - '6379:6379'

  sqlite:
    image: nouchka/sqlite3
    volumes:
      - ./data:/data
    command: sqlite3 /data/mydb.db
    ports:
      - '8080:8080'
