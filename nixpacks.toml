[phases.install]
cmds = [
  "python3 -m ensurepip",
  "python3 -m pip install flask yt-dlp requests pillow gunicorn --break-system-packages"
]

[start]
cmd = "gunicorn app:app --bind 0.0.0.0:$PORT --workers 1 --timeout 300"
